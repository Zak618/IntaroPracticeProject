{% extends 'base.html.twig' %}

{% block title %}Редактирование профиля{% endblock %}

{% block body %}
    <div class="contaner">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <h1 class="text-center">Редактирование профиля</h1>

                {{ include('client/_form.html.twig', {'button_label': 'Сохранить'}) }}

                {# <a class="btn btn-secondary" href="{{ path('app_store') }}" style="background-color: white; color: blue;">в магазин</a> #}
            </div>
        </div>
    </div>

{% endblock %}
